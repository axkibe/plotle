/*
| This is an autogenerated file.
|
| DO NOT EDIT!
*/


/*
| Export
*/
var
	Action =
		Action || { };


/*
| Imports
*/
var
	JoobjProto,
	Jools;


/*
| Capsule
*/
(function( ) {
'use strict';


var
	_tag =
		689920682;


/*
| Constructor.
*/
var ItemDrag =
Action.ItemDrag =
	function(
		tag,
		v_origin,    // the item being dragged
		v_start,     // mouse down point on drag creation
		v_transItem  // the transient item while it is dragged
	)
{

/**/if( CHECK )
/**/{
/**/	if( tag !== _tag )
/**/	{
/**/		throw new Error(
/**/			'tag mismatch'
/**/		);
/**/	}
/**/}

	this.origin =
		v_origin;

	this.start =
		v_start;

	this.transItem =
		v_transItem;

	Jools.immute( this );
};


Jools.subclass(
	ItemDrag,
	Action.Action
);


/*
| Creates a new ItemDrag object.
*/
ItemDrag.create =
ItemDrag.prototype.create =
	function(
		// free strings
	)
{
	var
		inherit,
		v_origin,
		v_start,
		v_transItem;

	if( this !== ItemDrag )
	{
		inherit =
			this;

		v_origin =
			this.origin;

		v_start =
			this.start;

		v_transItem =
			this.transItem;
	}

	for(
		var a = 0, aZ = arguments.length;
		a < aZ;
		a += 2
	)
	{
		var
			arg =
				arguments[ a + 1 ];

		switch( arguments[ a ] )
		{
			case 'origin' :

				if( arg !== undefined )
				{
					v_origin =
						arg;
				}

				break;

			case 'start' :

				if( arg !== undefined )
				{
					v_start =
						arg;
				}

				break;

			case 'transItem' :

				if( arg !== undefined )
				{
					v_transItem =
						arg;
				}

				break;

			default :

/**/			if( CHECK )
/**/			{
/**/				throw new Error(
/**/					'invalid argument: ' + arguments[ a ]
/**/				);
/**/			}
		}
	}

/**/if( CHECK )
/**/{
/**/
/**/	if( v_origin === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute origin'
/**/		);
/**/	}
/**/
/**/	if( v_origin === null )
/**/	{
/**/		throw new Error(
/**/			'origin must not be null'
/**/		);
/**/	}
/**/
/**/	if( v_start === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute start'
/**/		);
/**/	}
/**/
/**/	if( v_start === null )
/**/	{
/**/		throw new Error(
/**/			'start must not be null'
/**/		);
/**/	}
/**/	if( v_start.reflect !== 'Point' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_transItem === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute transItem'
/**/		);
/**/	}
/**/
/**/	if( v_transItem === null )
/**/	{
/**/		throw new Error(
/**/			'transItem must not be null'
/**/		);
/**/	}
/**/}

	if(
		inherit
		&&
		v_origin.equals( inherit.origin )
		&&
		v_start.equals( inherit.start )
		&&
		v_transItem.equals( inherit.transItem )
	)
	{
		return inherit;
	}

	return (
		new ItemDrag(
			_tag,
			v_origin,
			v_start,
			v_transItem
		)
	);
};


/*
| Reflection.
*/
ItemDrag.prototype.reflect =
	'ItemDrag';


/*
| Sets values by path.
*/
ItemDrag.prototype.setPath =
	JoobjProto.setPath;


/*
| Gets values by path.
*/
ItemDrag.prototype.getPath =
	JoobjProto.getPath;


/*
| Checks for equal objects.
*/
ItemDrag.prototype.equals =
	function(
		obj
	)
{
	return this === obj;
};


} )( );
