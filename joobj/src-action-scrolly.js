/*
| This is an autogenerated file.
|
| DO NOT EDIT!
*/


/*
| Export
*/
var
	Action =
		Action || { };


/*
| Imports
*/
var
	JoobjProto,
	Jools;


/*
| Capsule
*/
(function( ) {
'use strict';


var
	_tag =
		424956176;


/*
| Constructor.
*/
var ScrollY =
Action.ScrollY =
	function(
		tag,
		v_itemPath, // path to the item being scrolled
		v_start,    // mouse down point on start of scrolling
		v_startPos  // position of the scrollbar on start of scrolling
	)
{

/**/if( CHECK )
/**/{
/**/	if( tag !== _tag )
/**/	{
/**/		throw new Error(
/**/			'tag mismatch'
/**/		);
/**/	}
/**/}

	this.itemPath =
		v_itemPath;

	this.start =
		v_start;

	this.startPos =
		v_startPos;

	Jools.immute( this );
};


Jools.subclass(
	ScrollY,
	Action.Action
);


/*
| Creates a new ScrollY object.
*/
ScrollY.create =
ScrollY.prototype.create =
	function(
		// free strings
	)
{
	var
		inherit,
		v_itemPath,
		v_start,
		v_startPos;

	if( this !== ScrollY )
	{
		inherit =
			this;

		v_itemPath =
			this.itemPath;

		v_start =
			this.start;

		v_startPos =
			this.startPos;
	}

	for(
		var a = 0, aZ = arguments.length;
		a < aZ;
		a += 2
	)
	{
		var
			arg =
				arguments[ a + 1 ];

		switch( arguments[ a ] )
		{
			case 'itemPath' :

				if( arg !== undefined )
				{
					v_itemPath =
						arg;
				}

				break;

			case 'start' :

				if( arg !== undefined )
				{
					v_start =
						arg;
				}

				break;

			case 'startPos' :

				if( arg !== undefined )
				{
					v_startPos =
						arg;
				}

				break;

			default :

/**/			if( CHECK )
/**/			{
/**/				throw new Error(
/**/					'invalid argument: ' + arguments[ a ]
/**/				);
/**/			}
		}
	}

/**/if( CHECK )
/**/{
/**/
/**/	if( v_itemPath === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute itemPath'
/**/		);
/**/	}
/**/
/**/	if( v_itemPath === null )
/**/	{
/**/		throw new Error(
/**/			'itemPath must not be null'
/**/		);
/**/	}
/**/	if( v_itemPath.reflect !== 'Path' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_start === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute start'
/**/		);
/**/	}
/**/
/**/	if( v_start === null )
/**/	{
/**/		throw new Error(
/**/			'start must not be null'
/**/		);
/**/	}
/**/	if( v_start.reflect !== 'Point' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_startPos === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute startPos'
/**/		);
/**/	}
/**/
/**/	if( v_startPos === null )
/**/	{
/**/		throw new Error(
/**/			'startPos must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_startPos ) !== 'number'
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/}

	if(
		inherit
		&&
		v_itemPath.equals( inherit.itemPath )
		&&
		v_start.equals( inherit.start )
		&&
		v_startPos === inherit.startPos
	)
	{
		return inherit;
	}

	return (
		new ScrollY(
			_tag,
			v_itemPath,
			v_start,
			v_startPos
		)
	);
};


/*
| Reflection.
*/
ScrollY.prototype.reflect =
	'ScrollY';


/*
| Sets values by path.
*/
ScrollY.prototype.setPath =
	JoobjProto.setPath;


/*
| Gets values by path.
*/
ScrollY.prototype.getPath =
	JoobjProto.getPath;


/*
| Checks for equal objects.
*/
ScrollY.prototype.equals =
	function(
		obj
	)
{
	return this === obj;
};


} )( );
