/*
| This is an autogenerated file.
|
| DO NOT EDIT!
*/


/*
| Export
*/
var
	HoverReply;


/*
| Imports
*/
var
	JoobjProto,
	Jools;


/*
| Capsule
*/
(function( ) {
'use strict';


var
	_tag =
		985482829;


/*
| Constructor.
*/
HoverReply =
	function(
		tag,
		v_cursor, // the cursor to display
		v_path    // the path to the thing being hovered upon
	)
{

/**/if( CHECK )
/**/{
/**/	if( tag !== _tag )
/**/	{
/**/		throw new Error(
/**/			'tag mismatch'
/**/		);
/**/	}
/**/}

	this.cursor =
		v_cursor;

	this.path =
		v_path;

	Jools.immute( this );
};


/*
| Creates a new HoverReply object.
*/
HoverReply.create =
HoverReply.prototype.create =
	function(
		// free strings
	)
{
	var
		inherit,
		v_cursor,
		v_path;

	if( this !== HoverReply )
	{
		inherit =
			this;

		v_cursor =
			this.cursor;

		v_path =
			this.path;
	}

	for(
		var a = 0, aZ = arguments.length;
		a < aZ;
		a += 2
	)
	{
		var
			arg =
				arguments[ a + 1 ];

		switch( arguments[ a ] )
		{
			case 'cursor' :

				if( arg !== undefined )
				{
					v_cursor =
						arg;
				}

				break;

			case 'path' :

				if( arg !== undefined )
				{
					v_path =
						arg;
				}

				break;

			default :

/**/			if( CHECK )
/**/			{
/**/				throw new Error(
/**/					'invalid argument: ' + arguments[ a ]
/**/				);
/**/			}
		}
	}

/**/if( CHECK )
/**/{
/**/
/**/	if( v_cursor === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute cursor'
/**/		);
/**/	}
/**/
/**/	if( v_cursor === null )
/**/	{
/**/		throw new Error(
/**/			'cursor must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_cursor ) !== 'string'
/**/		&&
/**/		!( v_cursor instanceof String )
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_path === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute path'
/**/		);
/**/	}
/**/
/**/	if( v_path === null )
/**/	{
/**/		throw new Error(
/**/			'path must not be null'
/**/		);
/**/	}
/**/	if( v_path.reflect !== 'Path' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/}

	if(
		inherit
		&&
		v_cursor === inherit.cursor
		&&
		v_path.equals( inherit.path )
	)
	{
		return inherit;
	}

	return (
		new HoverReply(
			_tag,
			v_cursor,
			v_path
		)
	);
};


/*
| Reflection.
*/
HoverReply.prototype.reflect =
	'HoverReply';


/*
| Sets values by path.
*/
HoverReply.prototype.setPath =
	JoobjProto.setPath;


/*
| Gets values by path.
*/
HoverReply.prototype.getPath =
	JoobjProto.getPath;


/*
| Checks for equal objects.
*/
HoverReply.prototype.equals =
	function(
		obj
	)
{
	if( this === obj )
	{
		return true;
	}

	if( !obj )
	{
		return false;
	}

	return (
		this.cursor === obj.cursor
		&&
		this.path.equals( obj.path )
	);
};


} )( );
