/*
| This is an autogenerated file.
|
| DO NOT EDIT!
*/


/*
| Export
*/
var
	Euclid =
		Euclid || { };


/*
| Imports
*/
var
	JoobjProto,
	Jools;


/*
| Capsule
*/
(function( ) {
'use strict';


var
	_tag =
		848476998;


/*
| Node includes
*/
if( SERVER )
{
	JoobjProto =
		require( '../src/joobj/proto' );

	Jools =
		require( '../src/jools/jools' );
}


/*
| Constructor.
*/
var Point =
Euclid.Point =
	function(
		tag,
		v_x, // x coordinate
		v_y  // y coordinate
	)
{

/**/if( CHECK )
/**/{
/**/	if( tag !== _tag )
/**/	{
/**/		throw new Error(
/**/			'tag mismatch'
/**/		);
/**/	}
/**/}

	this.x =
		v_x;

	this.y =
		v_y;

	Jools.immute( this );
};


/*
| Creates a new Point object.
*/
Point.create =
Point.prototype.create =
	function(
		// free strings
	)
{
	var
		inherit,
		v_x,
		v_y;

	if( this !== Point )
	{
		inherit =
			this;

		v_x =
			this.x;

		v_y =
			this.y;
	}

	for(
		var a = 0, aZ = arguments.length;
		a < aZ;
		a += 2
	)
	{
		var
			arg =
				arguments[ a + 1 ];

		switch( arguments[ a ] )
		{
			case 'x' :

				if( arg !== undefined )
				{
					v_x =
						arg;
				}

				break;

			case 'y' :

				if( arg !== undefined )
				{
					v_y =
						arg;
				}

				break;

			default :

/**/			if( CHECK )
/**/			{
/**/				throw new Error(
/**/					'invalid argument: ' + arguments[ a ]
/**/				);
/**/			}
		}
	}

/**/if( CHECK )
/**/{
/**/
/**/	if( v_x === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute x'
/**/		);
/**/	}
/**/
/**/	if( v_x === null )
/**/	{
/**/		throw new Error(
/**/			'x must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_x ) !== 'number'
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_y === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute y'
/**/		);
/**/	}
/**/
/**/	if( v_y === null )
/**/	{
/**/		throw new Error(
/**/			'y must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_y ) !== 'number'
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/}

	if(
		inherit
		&&
		v_x === inherit.x
		&&
		v_y === inherit.y
	)
	{
		return inherit;
	}

	return (
		new Point(
			_tag,
			v_x,
			v_y
		)
	);
};


/*
| Creates a new Point object from JSON
*/
Point.createFromJSON =
	function(
		json // the json object
	)
{
	if( json._$grown ) return json;

	var
		v_x,
		v_y;

	for( var aName in json )
	{
		var
			arg =
				json[ aName ];

		switch( aName )
		{
			case 'type' :

				if( arg !== 'Point')
				{
					throw new Error(
						'invalid JSON'
					);
				}

				break;

			case 'x' :

				v_x =
					arg;

				break;


			case 'y' :

				v_y =
					arg;

				break;

			default :

				throw new Error(
					'invalid JSON: ' + aName
				);
		}
	}


/**/if( CHECK )
/**/{
/**/
/**/	if( v_x === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute x'
/**/		);
/**/	}
/**/
/**/	if( v_x === null )
/**/	{
/**/		throw new Error(
/**/			'x must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_x ) !== 'number'
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_y === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute y'
/**/		);
/**/	}
/**/
/**/	if( v_y === null )
/**/	{
/**/		throw new Error(
/**/			'y must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_y ) !== 'number'
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/}
	return (
		new Point(
			_tag,
			v_x,
			v_y
		)
	);
};


/*
| Reflection.
*/
Point.prototype.reflect =
	'Point';


/*
| Workaround meshverse growing
*/
Point.prototype._$grown =
	true;


/*
| Sets values by path.
*/
Point.prototype.setPath =
	JoobjProto.setPath;


/*
| Gets values by path.
*/
Point.prototype.getPath =
	JoobjProto.getPath;


/*
| Convers the object into a JSON.
*/
Jools.lazyValue(
	Point.prototype,
	'toJSON',
	function( )
	{
		var
			json;

		json =
			Object.freeze( {
				type :
					'Point',
				'x' :
					this.x,
				'y' :
					this.y
		} );

		return function( ) { return json; };
	}
);


/*
| Checks for equal objects.
*/
Point.prototype.equals =
	function(
		obj
	)
{
	if( this === obj )
	{
		return true;
	}

	if( !obj )
	{
		return false;
	}

	return (
		this.x === obj.x
		&&
		this.y === obj.y
	);
};


/*
| Node export
*/
if( SERVER )
{
	module.exports =
		Point;
}


} )( );
