/*
| This is an autogenerated file.
|
| DO NOT EDIT!
*/


/*
| Export
*/
var
	Widgets =
		Widgets || { };


/*
| Imports
*/
var
	JoobjProto,
	Jools;


/*
| Capsule
*/
(function( ) {
'use strict';


var
	_tag =
		900543858;


/*
| Constructor.
*/
var Input =
Widgets.Input =
	function(
		tag,
		v_designFrame, // designed frame (using anchors
		v_focusAccent, // true if the widget got focus
		v_font,        // font of the text
		v_hover,       // component hovered upon
		v_mark,        // the users mark
		v_maxlen,      // maximum input length
		v_password,    // true for password input
		v_path,        // the path of the widget
		v_style,       // name of the style used
		v_superFrame,  // the frame the widget resides in
		v_value,       // the value in the input box
		v_visible      // if false the button is hidden
	)
{

/**/if( CHECK )
/**/{
/**/	if( tag !== _tag )
/**/	{
/**/		throw new Error(
/**/			'tag mismatch'
/**/		);
/**/	}
/**/}

	this.designFrame =
		v_designFrame;

	this.focusAccent =
		v_focusAccent;

	this.font =
		v_font;

	this.hover =
		v_hover;

	this.mark =
		v_mark;

	this.maxlen =
		v_maxlen;

	this.password =
		v_password;

	this.path =
		v_path;

	this.style =
		v_style;

	this.superFrame =
		v_superFrame;

	this.value =
		v_value;

	this.visible =
		v_visible;

	this._init( );

	Jools.immute( this );
};


Jools.subclass(
	Input,
	Widgets.Widget
);


/*
| Creates a new Input object.
*/
Input.create =
Input.prototype.create =
	function(
		// free strings
	)
{
	var
		inherit,
		v_designFrame,
		v_focusAccent,
		v_font,
		v_hover,
		v_mark,
		v_maxlen,
		v_password,
		v_path,
		v_style,
		v_superFrame,
		v_value,
		v_visible;

	if( this !== Input )
	{
		inherit =
			this;

		v_designFrame =
			this.designFrame;

		v_focusAccent =
			this.focusAccent;

		v_font =
			this.font;

		v_hover =
			this.hover;

		v_mark =
			this.mark;

		v_maxlen =
			this.maxlen;

		v_password =
			this.password;

		v_path =
			this.path;

		v_style =
			this.style;

		v_superFrame =
			this.superFrame;

		v_value =
			this.value;

		v_visible =
			this.visible;
	}

	for(
		var a = 0, aZ = arguments.length;
		a < aZ;
		a += 2
	)
	{
		var
			arg =
				arguments[ a + 1 ];

		switch( arguments[ a ] )
		{
			case 'designFrame' :

				if( arg !== undefined )
				{
					v_designFrame =
						arg;
				}

				break;

			case 'focusAccent' :

				if( arg !== undefined )
				{
					v_focusAccent =
						arg;
				}

				break;

			case 'font' :

				if( arg !== undefined )
				{
					v_font =
						arg;
				}

				break;

			case 'hover' :

				if( arg !== undefined )
				{
					v_hover =
						arg;
				}

				break;

			case 'mark' :

				if( arg !== undefined )
				{
					v_mark =
						arg;
				}

				break;

			case 'maxlen' :

				if( arg !== undefined )
				{
					v_maxlen =
						arg;
				}

				break;

			case 'password' :

				if( arg !== undefined )
				{
					v_password =
						arg;
				}

				break;

			case 'path' :

				if( arg !== undefined )
				{
					v_path =
						arg;
				}

				break;

			case 'style' :

				if( arg !== undefined )
				{
					v_style =
						arg;
				}

				break;

			case 'superFrame' :

				if( arg !== undefined )
				{
					v_superFrame =
						arg;
				}

				break;

			case 'value' :

				if( arg !== undefined )
				{
					v_value =
						arg;
				}

				break;

			case 'visible' :

				if( arg !== undefined )
				{
					v_visible =
						arg;
				}

				break;

			default :

/**/			if( CHECK )
/**/			{
/**/				throw new Error(
/**/					'invalid argument: ' + arguments[ a ]
/**/				);
/**/			}
		}
	}

	if( v_focusAccent === undefined )
	{
		v_focusAccent =
			false;
	}

	if( v_hover === undefined )
	{
		v_hover =
			null;
	}

	if( v_mark === undefined )
	{
		v_mark =
			null;
	}

	if( v_password === undefined )
	{
		v_password =
			false;
	}

	if( v_path === undefined )
	{
		v_path =
			null;
	}

	if( v_superFrame === undefined )
	{
		v_superFrame =
			null;
	}

	if( v_value === undefined )
	{
		v_value =
			'';
	}

	if( v_visible === undefined )
	{
		v_visible =
			true;
	}

/**/if( CHECK )
/**/{
/**/
/**/	if( v_designFrame === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute designFrame'
/**/		);
/**/	}
/**/
/**/	if( v_designFrame === null )
/**/	{
/**/		throw new Error(
/**/			'designFrame must not be null'
/**/		);
/**/	}
/**/	if( v_designFrame.reflect !== 'AnchorRect' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_focusAccent === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute focusAccent'
/**/		);
/**/	}
/**/
/**/	if( v_focusAccent === null )
/**/	{
/**/		throw new Error(
/**/			'focusAccent must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_focusAccent ) !== 'boolean'
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_font === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute font'
/**/		);
/**/	}
/**/
/**/	if( v_font === null )
/**/	{
/**/		throw new Error(
/**/			'font must not be null'
/**/		);
/**/	}
/**/	if( v_font.reflect !== 'Font' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_hover === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute hover'
/**/		);
/**/	}
/**/
/**/	if( v_hover !== null )
/**/	{
/**/		if( v_hover.reflect !== 'Path' )
/**/		{
/**/			throw new Error(
/**/				'type mismatch'
/**/			);
/**/		}
/**/	}
/**/
/**/	if( v_mark === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute mark'
/**/		);
/**/	}
/**/
/**/	if( v_maxlen === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute maxlen'
/**/		);
/**/	}
/**/
/**/	if( v_maxlen === null )
/**/	{
/**/		throw new Error(
/**/			'maxlen must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_maxlen ) !== 'number'
/**/		||
/**/		Math.floor( v_maxlen ) !== v_maxlen
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_password === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute password'
/**/		);
/**/	}
/**/
/**/	if( v_password === null )
/**/	{
/**/		throw new Error(
/**/			'password must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_password ) !== 'boolean'
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_path === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute path'
/**/		);
/**/	}
/**/
/**/	if( v_path !== null )
/**/	{
/**/		if( v_path.reflect !== 'Path' )
/**/		{
/**/			throw new Error(
/**/				'type mismatch'
/**/			);
/**/		}
/**/	}
/**/
/**/	if( v_style === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute style'
/**/		);
/**/	}
/**/
/**/	if( v_style === null )
/**/	{
/**/		throw new Error(
/**/			'style must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_style ) !== 'string'
/**/		&&
/**/		!( v_style instanceof String )
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_superFrame === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute superFrame'
/**/		);
/**/	}
/**/
/**/	if( v_superFrame !== null )
/**/	{
/**/		if( v_superFrame.reflect !== 'Rect' )
/**/		{
/**/			throw new Error(
/**/				'type mismatch'
/**/			);
/**/		}
/**/	}
/**/
/**/	if( v_value === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute value'
/**/		);
/**/	}
/**/
/**/	if( v_value === null )
/**/	{
/**/		throw new Error(
/**/			'value must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_value ) !== 'string'
/**/		&&
/**/		!( v_value instanceof String )
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_visible === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute visible'
/**/		);
/**/	}
/**/
/**/	if( v_visible === null )
/**/	{
/**/		throw new Error(
/**/			'visible must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_visible ) !== 'boolean'
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/}
	v_hover =
		Widgets.Widget.concernsHover(
			v_hover,
			v_path
		);

	v_mark =
		Widgets.Widget.concernsMark(
			v_mark,
			v_path
		);


	if(
		inherit
		&&
		v_designFrame.equals( inherit.designFrame )
		&&
		v_focusAccent === inherit.focusAccent
		&&
		v_font.equals( inherit.font )
		&&
		(
			v_hover === inherit.hover
			||
			(
				v_hover
				&&
				v_hover.equals( inherit.hover )
			)
		)
		&&
		v_mark === inherit.mark
		&&
		v_maxlen === inherit.maxlen
		&&
		v_password === inherit.password
		&&
		(
			v_path === inherit.path
			||
			(
				v_path
				&&
				v_path.equals( inherit.path )
			)
		)
		&&
		v_style === inherit.style
		&&
		(
			v_superFrame === inherit.superFrame
			||
			(
				v_superFrame
				&&
				v_superFrame.equals( inherit.superFrame )
			)
		)
		&&
		v_value === inherit.value
		&&
		v_visible === inherit.visible
	)
	{
		return inherit;
	}

	return (
		new Input(
			_tag,
			v_designFrame,
			v_focusAccent,
			v_font,
			v_hover,
			v_mark,
			v_maxlen,
			v_password,
			v_path,
			v_style,
			v_superFrame,
			v_value,
			v_visible
		)
	);
};


/*
| Reflection.
*/
Input.prototype.reflect =
	'Input';


/*
| Sets values by path.
*/
Input.prototype.setPath =
	JoobjProto.setPath;


/*
| Gets values by path.
*/
Input.prototype.getPath =
	JoobjProto.getPath;


/*
| Checks for equal objects.
*/
Input.prototype.equals =
	function(
		obj
	)
{
	if( this === obj )
	{
		return true;
	}

	if( !obj )
	{
		return false;
	}

	return (
		this.designFrame.equals( obj.designFrame )
		&&
		this.focusAccent === obj.focusAccent
		&&
		this.font.equals( obj.font )
		&&
		(
			this.hover === obj.hover ||
			(
				this.hover !== null
				&&
				this.hover.equals( obj.hover )
			)
		)
		&&
		this.mark === obj.mark
		&&
		this.maxlen === obj.maxlen
		&&
		this.password === obj.password
		&&
		(
			this.path === obj.path ||
			(
				this.path !== null
				&&
				this.path.equals( obj.path )
			)
		)
		&&
		this.style === obj.style
		&&
		(
			this.superFrame === obj.superFrame ||
			(
				this.superFrame !== null
				&&
				this.superFrame.equals( obj.superFrame )
			)
		)
		&&
		this.value === obj.value
		&&
		this.visible === obj.visible
	);
};


} )( );
