/*
| This is an autogenerated file.
|
| DO NOT EDIT!
*/


/*
| Export
*/
var
	Design =
		Design || { };


/*
| Imports
*/
var
	JoobjProto,
	Jools;


/*
| Capsule
*/
(function( ) {
'use strict';


var
	_tag =
		230666923;


/*
| Constructor.
*/
var AnchorRect =
Design.AnchorRect =
	function(
		tag,
		v_pnw, // point in north-west
		v_pse  // point in south-east
	)
{

/**/if( CHECK )
/**/{
/**/	if( tag !== _tag )
/**/	{
/**/		throw new Error(
/**/			'tag mismatch'
/**/		);
/**/	}
/**/}

	this.pnw =
		v_pnw;

	this.pse =
		v_pse;

	Jools.immute( this );
};


/*
| Creates a new AnchorRect object.
*/
AnchorRect.create =
AnchorRect.prototype.create =
	function(
		// free strings
	)
{
	var
		inherit,
		v_pnw,
		v_pse;

	if( this !== AnchorRect )
	{
		inherit =
			this;

		v_pnw =
			this.pnw;

		v_pse =
			this.pse;
	}

	for(
		var a = 0, aZ = arguments.length;
		a < aZ;
		a += 2
	)
	{
		var
			arg =
				arguments[ a + 1 ];

		switch( arguments[ a ] )
		{
			case 'pnw' :

				if( arg !== undefined )
				{
					v_pnw =
						arg;
				}

				break;

			case 'pse' :

				if( arg !== undefined )
				{
					v_pse =
						arg;
				}

				break;

			default :

/**/			if( CHECK )
/**/			{
/**/				throw new Error(
/**/					'invalid argument: ' + arguments[ a ]
/**/				);
/**/			}
		}
	}

/**/if( CHECK )
/**/{
/**/
/**/	if( v_pnw === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute pnw'
/**/		);
/**/	}
/**/
/**/	if( v_pnw === null )
/**/	{
/**/		throw new Error(
/**/			'pnw must not be null'
/**/		);
/**/	}
/**/	if( v_pnw.reflect !== 'AnchorPoint' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_pse === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute pse'
/**/		);
/**/	}
/**/
/**/	if( v_pse === null )
/**/	{
/**/		throw new Error(
/**/			'pse must not be null'
/**/		);
/**/	}
/**/	if( v_pse.reflect !== 'AnchorPoint' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/}

	if(
		inherit
		&&
		v_pnw.equals( inherit.pnw )
		&&
		v_pse.equals( inherit.pse )
	)
	{
		return inherit;
	}

	return (
		new AnchorRect(
			_tag,
			v_pnw,
			v_pse
		)
	);
};


/*
| Reflection.
*/
AnchorRect.prototype.reflect =
	'AnchorRect';


/*
| Sets values by path.
*/
AnchorRect.prototype.setPath =
	JoobjProto.setPath;


/*
| Gets values by path.
*/
AnchorRect.prototype.getPath =
	JoobjProto.getPath;


/*
| Checks for equal objects.
*/
AnchorRect.prototype.equals =
	function(
		obj
	)
{
	if( this === obj )
	{
		return true;
	}

	if( !obj )
	{
		return false;
	}

	return (
		this.pnw.equals( obj.pnw )
		&&
		this.pse.equals( obj.pse )
	);
};


} )( );
