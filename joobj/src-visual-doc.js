/*
| This is an autogenerated file.
|
| DO NOT EDIT!
*/


/*
| Export
*/
var
	Visual =
		Visual || { };


/*
| Imports
*/
var
	JoobjProto,
	Jools;


/*
| Capsule
*/
(function( ) {
'use strict';


var
	_tag =
		242468228;


/*
| Node includes
*/
if( SERVER )
{
	JoobjProto =
		require( '../src/joobj/proto' );

	Jools =
		require( '../src/jools/jools' );
}


/*
| Constructor.
*/
var Doc =
Visual.Doc =
	function(
		tag,
		v_flowWidth, // width of the para its flow
		v_fontsize,  // size of the font
		v_mark,      // the users mark
		v_paraSep,   // vertical seperation of paragraphs
		v_path,      // the path of the doc
		v_tree,      // the data tree
		v_view       // the current view
	)
{

/**/if( CHECK )
/**/{
/**/	if( tag !== _tag )
/**/	{
/**/		throw new Error(
/**/			'tag mismatch'
/**/		);
/**/	}
/**/}

	this.flowWidth =
		v_flowWidth;

	this.fontsize =
		v_fontsize;

	this.mark =
		v_mark;

	this.paraSep =
		v_paraSep;

	this.path =
		v_path;

	this.tree =
		v_tree;

	this.view =
		v_view;

	this._init( );

	Jools.immute( this );
};


/*
| Creates a new Doc object.
*/
Doc.create =
Doc.prototype.create =
	function(
		// free strings
	)
{
	var
		inherit,
		v_flowWidth,
		v_fontsize,
		v_mark,
		v_paraSep,
		v_path,
		v_tree,
		v_view;

	if( this !== Doc )
	{
		inherit =
			this;

		v_flowWidth =
			this.flowWidth;

		v_fontsize =
			this.fontsize;

		v_mark =
			this.mark;

		v_paraSep =
			this.paraSep;

		v_path =
			this.path;

		v_tree =
			this.tree;

		v_view =
			this.view;
	}

	for(
		var a = 0, aZ = arguments.length;
		a < aZ;
		a += 2
	)
	{
		var
			arg =
				arguments[ a + 1 ];

		switch( arguments[ a ] )
		{
			case 'flowWidth' :

				if( arg !== undefined )
				{
					v_flowWidth =
						arg;
				}

				break;

			case 'fontsize' :

				if( arg !== undefined )
				{
					v_fontsize =
						arg;
				}

				break;

			case 'mark' :

				if( arg !== undefined )
				{
					v_mark =
						arg;
				}

				break;

			case 'paraSep' :

				if( arg !== undefined )
				{
					v_paraSep =
						arg;
				}

				break;

			case 'path' :

				if( arg !== undefined )
				{
					v_path =
						arg;
				}

				break;

			case 'tree' :

				if( arg !== undefined )
				{
					v_tree =
						arg;
				}

				break;

			case 'view' :

				if( arg !== undefined )
				{
					v_view =
						arg;
				}

				break;

			default :

/**/			if( CHECK )
/**/			{
/**/				throw new Error(
/**/					'invalid argument: ' + arguments[ a ]
/**/				);
/**/			}
		}
	}

/**/if( CHECK )
/**/{
/**/
/**/	if( v_flowWidth === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute flowWidth'
/**/		);
/**/	}
/**/
/**/	if( v_flowWidth === null )
/**/	{
/**/		throw new Error(
/**/			'flowWidth must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_flowWidth ) !== 'number'
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_fontsize === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute fontsize'
/**/		);
/**/	}
/**/
/**/	if( v_fontsize === null )
/**/	{
/**/		throw new Error(
/**/			'fontsize must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_fontsize ) !== 'number'
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_mark === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute mark'
/**/		);
/**/	}
/**/
/**/	if( v_mark === null )
/**/	{
/**/		throw new Error(
/**/			'mark must not be null'
/**/		);
/**/	}
/**/
/**/	if( v_paraSep === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute paraSep'
/**/		);
/**/	}
/**/
/**/	if( v_paraSep === null )
/**/	{
/**/		throw new Error(
/**/			'paraSep must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_paraSep ) !== 'number'
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_path === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute path'
/**/		);
/**/	}
/**/
/**/	if( v_path === null )
/**/	{
/**/		throw new Error(
/**/			'path must not be null'
/**/		);
/**/	}
/**/	if( v_path.reflect !== 'Path' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_tree === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute tree'
/**/		);
/**/	}
/**/
/**/	if( v_tree === null )
/**/	{
/**/		throw new Error(
/**/			'tree must not be null'
/**/		);
/**/	}
/**/
/**/	if( v_view === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute view'
/**/		);
/**/	}
/**/
/**/	if( v_view === null )
/**/	{
/**/		throw new Error(
/**/			'view must not be null'
/**/		);
/**/	}
/**/	if( v_view.reflect !== 'View' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/}

	if(
		inherit
		&&
		v_flowWidth === inherit.flowWidth
		&&
		v_fontsize === inherit.fontsize
		&&
		v_mark === inherit.mark
		&&
		v_paraSep === inherit.paraSep
		&&
		v_path.equals( inherit.path )
		&&
		v_tree === inherit.tree
		&&
		v_view.equals( inherit.view )
	)
	{
		return inherit;
	}

	return (
		new Doc(
			_tag,
			v_flowWidth,
			v_fontsize,
			v_mark,
			v_paraSep,
			v_path,
			v_tree,
			v_view
		)
	);
};


/*
| Reflection.
*/
Doc.prototype.reflect =
	'Doc';


/*
| Sets values by path.
*/
Doc.prototype.setPath =
	JoobjProto.setPath;


/*
| Gets values by path.
*/
Doc.prototype.getPath =
	JoobjProto.getPath;


/*
| Checks for equal objects.
*/
Doc.prototype.equals =
	function(
		obj
	)
{
	if( this === obj )
	{
		return true;
	}

	if( !obj )
	{
		return false;
	}

	return (
		this.flowWidth === obj.flowWidth
		&&
		this.fontsize === obj.fontsize
		&&
		this.mark === obj.mark
		&&
		this.paraSep === obj.paraSep
		&&
		this.path.equals( obj.path )
		&&
		this.tree === obj.tree
		&&
		this.view.equals( obj.view )
	);
};


/*
| Node export
*/
if( SERVER )
{
	module.exports =
		Doc;
}


} )( );
