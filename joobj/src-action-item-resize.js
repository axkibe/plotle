/*
| This is an autogenerated file.
|
| DO NOT EDIT!
*/


/*
| Export
*/
var
	Action =
		Action || { };


/*
| Imports
*/
var
	JoobjProto,
	Jools;


/*
| Capsule
*/
(function( ) {
'use strict';


var
	_tag =
		771447393;


/*
| Constructor.
*/
var ItemResize =
Action.ItemResize =
	function(
		tag,
		v_align,     // alignment ( compass ) of the resize action
		v_origin,    // the item being resized
		v_start,     // mouseDown point on drag creation
		v_transItem  // the transient item while it is dragged
	)
{

/**/if( CHECK )
/**/{
/**/	if( tag !== _tag )
/**/	{
/**/		throw new Error(
/**/			'tag mismatch'
/**/		);
/**/	}
/**/}

	this.align =
		v_align;

	this.origin =
		v_origin;

	this.start =
		v_start;

	this.transItem =
		v_transItem;

	Jools.immute( this );
};


Jools.subclass(
	ItemResize,
	Action.Action
);


/*
| Creates a new ItemResize object.
*/
ItemResize.create =
ItemResize.prototype.create =
	function(
		// free strings
	)
{
	var
		inherit,
		v_align,
		v_origin,
		v_start,
		v_transItem;

	if( this !== ItemResize )
	{
		inherit =
			this;

		v_align =
			this.align;

		v_origin =
			this.origin;

		v_start =
			this.start;

		v_transItem =
			this.transItem;
	}

	for(
		var a = 0, aZ = arguments.length;
		a < aZ;
		a += 2
	)
	{
		var
			arg =
				arguments[ a + 1 ];

		switch( arguments[ a ] )
		{
			case 'align' :

				if( arg !== undefined )
				{
					v_align =
						arg;
				}

				break;

			case 'origin' :

				if( arg !== undefined )
				{
					v_origin =
						arg;
				}

				break;

			case 'start' :

				if( arg !== undefined )
				{
					v_start =
						arg;
				}

				break;

			case 'transItem' :

				if( arg !== undefined )
				{
					v_transItem =
						arg;
				}

				break;

			default :

/**/			if( CHECK )
/**/			{
/**/				throw new Error(
/**/					'invalid argument: ' + arguments[ a ]
/**/				);
/**/			}
		}
	}

/**/if( CHECK )
/**/{
/**/
/**/	if( v_align === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute align'
/**/		);
/**/	}
/**/
/**/	if( v_align === null )
/**/	{
/**/		throw new Error(
/**/			'align must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_align ) !== 'string'
/**/		&&
/**/		!( v_align instanceof String )
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_origin === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute origin'
/**/		);
/**/	}
/**/
/**/	if( v_origin === null )
/**/	{
/**/		throw new Error(
/**/			'origin must not be null'
/**/		);
/**/	}
/**/
/**/	if( v_start === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute start'
/**/		);
/**/	}
/**/
/**/	if( v_start === null )
/**/	{
/**/		throw new Error(
/**/			'start must not be null'
/**/		);
/**/	}
/**/	if( v_start.reflect !== 'Point' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_transItem === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute transItem'
/**/		);
/**/	}
/**/
/**/	if( v_transItem === null )
/**/	{
/**/		throw new Error(
/**/			'transItem must not be null'
/**/		);
/**/	}
/**/}

	if(
		inherit
		&&
		v_align === inherit.align
		&&
		v_origin.equals( inherit.origin )
		&&
		v_start.equals( inherit.start )
		&&
		v_transItem.equals( inherit.transItem )
	)
	{
		return inherit;
	}

	return (
		new ItemResize(
			_tag,
			v_align,
			v_origin,
			v_start,
			v_transItem
		)
	);
};


/*
| Reflection.
*/
ItemResize.prototype.reflect =
	'ItemResize';


/*
| Sets values by path.
*/
ItemResize.prototype.setPath =
	JoobjProto.setPath;


/*
| Gets values by path.
*/
ItemResize.prototype.getPath =
	JoobjProto.getPath;


/*
| Checks for equal objects.
*/
ItemResize.prototype.equals =
	function(
		obj
	)
{
	return this === obj;
};


} )( );
