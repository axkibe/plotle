/*
| This is an autogenerated file.
|
| DO NOT EDIT!
*/


/*
| Export
*/
var
	Action =
		Action || { };


/*
| Imports
*/
var
	JoobjProto,
	Jools;


/*
| Capsule
*/
(function( ) {
'use strict';


var
	_tag =
		47575701;


/*
| Constructor.
*/
var CreateGeneric =
Action.CreateGeneric =
	function(
		tag,
		v_itemType,  // item type to be created
		v_model,     // a transient item used as model
		v_start,     // mouse down point on drag creation
		v_transItem  // the transient item in creation
	)
{

/**/if( CHECK )
/**/{
/**/	if( tag !== _tag )
/**/	{
/**/		throw new Error(
/**/			'tag mismatch'
/**/		);
/**/	}
/**/}

	this.itemType =
		v_itemType;

	this.model =
		v_model;

	this.start =
		v_start;

	this.transItem =
		v_transItem;

	Jools.immute( this );
};


Jools.subclass(
	CreateGeneric,
	Action.Action
);


/*
| Creates a new CreateGeneric object.
*/
CreateGeneric.create =
CreateGeneric.prototype.create =
	function(
		// free strings
	)
{
	var
		inherit,
		v_itemType,
		v_model,
		v_start,
		v_transItem;

	if( this !== CreateGeneric )
	{
		inherit =
			this;

		v_itemType =
			this.itemType;

		v_model =
			this.model;

		v_start =
			this.start;

		v_transItem =
			this.transItem;
	}

	for(
		var a = 0, aZ = arguments.length;
		a < aZ;
		a += 2
	)
	{
		var
			arg =
				arguments[ a + 1 ];

		switch( arguments[ a ] )
		{
			case 'itemType' :

				if( arg !== undefined )
				{
					v_itemType =
						arg;
				}

				break;

			case 'model' :

				if( arg !== undefined )
				{
					v_model =
						arg;
				}

				break;

			case 'start' :

				if( arg !== undefined )
				{
					v_start =
						arg;
				}

				break;

			case 'transItem' :

				if( arg !== undefined )
				{
					v_transItem =
						arg;
				}

				break;

			default :

/**/			if( CHECK )
/**/			{
/**/				throw new Error(
/**/					'invalid argument: ' + arguments[ a ]
/**/				);
/**/			}
		}
	}

/**/if( CHECK )
/**/{
/**/
/**/	if( v_itemType === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute itemType'
/**/		);
/**/	}
/**/
/**/	if( v_itemType === null )
/**/	{
/**/		throw new Error(
/**/			'itemType must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_itemType ) !== 'string'
/**/		&&
/**/		!( v_itemType instanceof String )
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_model === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute model'
/**/		);
/**/	}
/**/
/**/	if( v_start === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute start'
/**/		);
/**/	}
/**/
/**/	if( v_start !== null )
/**/	{
/**/		if( v_start.reflect !== 'Point' )
/**/		{
/**/			throw new Error(
/**/				'type mismatch'
/**/			);
/**/		}
/**/	}
/**/
/**/	if( v_transItem === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute transItem'
/**/		);
/**/	}
/**/}

	if(
		inherit
		&&
		v_itemType === inherit.itemType
		&&
		(
			v_model === inherit.model
			||
			(
				v_model
				&&
				v_model.equals( inherit.model )
			)
		)
		&&
		(
			v_start === inherit.start
			||
			(
				v_start
				&&
				v_start.equals( inherit.start )
			)
		)
		&&
		(
			v_transItem === inherit.transItem
			||
			(
				v_transItem
				&&
				v_transItem.equals( inherit.transItem )
			)
		)
	)
	{
		return inherit;
	}

	return (
		new CreateGeneric(
			_tag,
			v_itemType,
			v_model,
			v_start,
			v_transItem
		)
	);
};


/*
| Reflection.
*/
CreateGeneric.prototype.reflect =
	'CreateGeneric';


/*
| Sets values by path.
*/
CreateGeneric.prototype.setPath =
	JoobjProto.setPath;


/*
| Gets values by path.
*/
CreateGeneric.prototype.getPath =
	JoobjProto.getPath;


/*
| Checks for equal objects.
*/
CreateGeneric.prototype.equals =
	function(
		obj
	)
{
	return this === obj;
};


} )( );
