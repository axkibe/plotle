/*
| This is an autogenerated file.
|
| DO NOT EDIT!
*/


/*
| Export
*/
var
	Mark =
		Mark || { };


/*
| Imports
*/
var
	JoobjProto,
	Jools;


/*
| Capsule
*/
(function( ) {
'use strict';


var
	_tag =
		760690010;


/*
| Constructor.
*/
var Item =
Mark.Item =
	function(
		tag,
		v_path  // path of the item
	)
{

/**/if( CHECK )
/**/{
/**/	if( tag !== _tag )
/**/	{
/**/		throw new Error(
/**/			'tag mismatch'
/**/		);
/**/	}
/**/}

	this.path =
		v_path;

	Jools.immute( this );
};


Jools.subclass(
	Item,
	Mark.Mark
);


/*
| Creates a new Item object.
*/
Item.create =
Item.prototype.create =
	function(
		// free strings
	)
{
	var
		inherit,
		v_path;

	if( this !== Item )
	{
		inherit =
			this;

		v_path =
			this.path;
	}

	for(
		var a = 0, aZ = arguments.length;
		a < aZ;
		a += 2
	)
	{
		var
			arg =
				arguments[ a + 1 ];

		switch( arguments[ a ] )
		{
			case 'path' :

				if( arg !== undefined )
				{
					v_path =
						arg;
				}

				break;

			default :

/**/			if( CHECK )
/**/			{
/**/				throw new Error(
/**/					'invalid argument: ' + arguments[ a ]
/**/				);
/**/			}
		}
	}

/**/if( CHECK )
/**/{
/**/
/**/	if( v_path === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute path'
/**/		);
/**/	}
/**/
/**/	if( v_path === null )
/**/	{
/**/		throw new Error(
/**/			'path must not be null'
/**/		);
/**/	}
/**/	if( v_path.reflect !== 'Path' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/}

	if(
		inherit
		&&
		v_path.equals( inherit.path )
	)
	{
		return inherit;
	}

	return (
		new Item(
			_tag,
			v_path
		)
	);
};


/*
| Reflection.
*/
Item.prototype.reflect =
	'Item';


/*
| Sets values by path.
*/
Item.prototype.setPath =
	JoobjProto.setPath;


/*
| Gets values by path.
*/
Item.prototype.getPath =
	JoobjProto.getPath;


/*
| Checks for equal objects.
*/
Item.prototype.equals =
	function(
		obj
	)
{
	if( this === obj )
	{
		return true;
	}

	if( !obj )
	{
		return false;
	}

	return (
		this.path.equals( obj.path )
	);
};


} )( );
