/*
| This is an autogenerated file.
|
| DO NOT EDIT!
*/


/*
| Export
*/
var
	Visual =
		Visual || { };


/*
| Imports
*/
var
	JoobjProto,
	Jools;


/*
| Capsule
*/
(function( ) {
'use strict';


var
	_tag =
		845622312;


/*
| Constructor.
*/
var Space =
Visual.Space =
	function(
		tag,
		inherit,     // inheritance
		v_access,    // rights the current user has for this space
		v_hover,     // node currently hovered upon
		v_mark,      // the users mark
		v_path,      // the path of the space
		v_spaceTag,  // name of the space
		v_spaceUser, // owner of the space
		v_traitSet,  // traitSet
		v_tree,      // the data tree
		v_view       // the current view
	)
{

/**/if( CHECK )
/**/{
/**/	if( tag !== _tag )
/**/	{
/**/		throw new Error(
/**/			'tag mismatch'
/**/		);
/**/	}
/**/}

	this.access =
		v_access;

	this.hover =
		v_hover;

	this.mark =
		v_mark;

	this.path =
		v_path;

	this.spaceTag =
		v_spaceTag;

	this.spaceUser =
		v_spaceUser;

	this.tree =
		v_tree;

	this.view =
		v_view;

	this._init(
		inherit,
		v_traitSet
	);

	Jools.immute( this );
};


/*
| Creates a new Space object.
*/
Space.create =
Space.prototype.create =
	function(
		// free strings
	)
{
	var
		inherit,
		v_access,
		v_hover,
		v_mark,
		v_path,
		v_spaceTag,
		v_spaceUser,
		v_traitSet,
		v_tree,
		v_view;

	if( this !== Space )
	{
		inherit =
			this;

		v_access =
			this.access;

		v_hover =
			this.hover;

		v_mark =
			this.mark;

		v_path =
			this.path;

		v_spaceTag =
			this.spaceTag;

		v_spaceUser =
			this.spaceUser;

		v_tree =
			this.tree;

		v_view =
			this.view;
	}

	for(
		var a = 0, aZ = arguments.length;
		a < aZ;
		a += 2
	)
	{
		var
			arg =
				arguments[ a + 1 ];

		switch( arguments[ a ] )
		{
			case 'access' :

				if( arg !== undefined )
				{
					v_access =
						arg;
				}

				break;

			case 'hover' :

				if( arg !== undefined )
				{
					v_hover =
						arg;
				}

				break;

			case 'mark' :

				if( arg !== undefined )
				{
					v_mark =
						arg;
				}

				break;

			case 'path' :

				if( arg !== undefined )
				{
					v_path =
						arg;
				}

				break;

			case 'spaceTag' :

				if( arg !== undefined )
				{
					v_spaceTag =
						arg;
				}

				break;

			case 'spaceUser' :

				if( arg !== undefined )
				{
					v_spaceUser =
						arg;
				}

				break;

			case 'traitSet' :

				if( arg !== undefined )
				{
					v_traitSet =
						arg;
				}

				break;

			case 'tree' :

				if( arg !== undefined )
				{
					v_tree =
						arg;
				}

				break;

			case 'view' :

				if( arg !== undefined )
				{
					v_view =
						arg;
				}

				break;

			default :

/**/			if( CHECK )
/**/			{
/**/				throw new Error(
/**/					'invalid argument: ' + arguments[ a ]
/**/				);
/**/			}
		}
	}

	if( v_traitSet === undefined )
	{
		v_traitSet =
			null;
	}

/**/if( CHECK )
/**/{
/**/
/**/	if( v_access === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute access'
/**/		);
/**/	}
/**/
/**/	if( v_access === null )
/**/	{
/**/		throw new Error(
/**/			'access must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_access ) !== 'string'
/**/		&&
/**/		!( v_access instanceof String )
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_hover === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute hover'
/**/		);
/**/	}
/**/
/**/	if( v_hover === null )
/**/	{
/**/		throw new Error(
/**/			'hover must not be null'
/**/		);
/**/	}
/**/	if( v_hover.reflect !== 'Path' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_mark === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute mark'
/**/		);
/**/	}
/**/
/**/	if( v_mark === null )
/**/	{
/**/		throw new Error(
/**/			'mark must not be null'
/**/		);
/**/	}
/**/
/**/	if( v_path === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute path'
/**/		);
/**/	}
/**/
/**/	if( v_path === null )
/**/	{
/**/		throw new Error(
/**/			'path must not be null'
/**/		);
/**/	}
/**/	if( v_path.reflect !== 'Path' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_spaceTag === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute spaceTag'
/**/		);
/**/	}
/**/
/**/	if( v_spaceTag === null )
/**/	{
/**/		throw new Error(
/**/			'spaceTag must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_spaceTag ) !== 'string'
/**/		&&
/**/		!( v_spaceTag instanceof String )
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_spaceUser === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute spaceUser'
/**/		);
/**/	}
/**/
/**/	if( v_spaceUser === null )
/**/	{
/**/		throw new Error(
/**/			'spaceUser must not be null'
/**/		);
/**/	}
/**/	if(
/**/		typeof( v_spaceUser ) !== 'string'
/**/		&&
/**/		!( v_spaceUser instanceof String )
/**/	)
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/
/**/	if( v_traitSet === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute traitSet'
/**/		);
/**/	}
/**/
/**/	if( v_traitSet !== null )
/**/	{
/**/		if( v_traitSet.reflect !== 'TraitSet' )
/**/		{
/**/			throw new Error(
/**/				'type mismatch'
/**/			);
/**/		}
/**/	}
/**/
/**/	if( v_tree === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute tree'
/**/		);
/**/	}
/**/
/**/	if( v_tree === null )
/**/	{
/**/		throw new Error(
/**/			'tree must not be null'
/**/		);
/**/	}
/**/
/**/	if( v_view === undefined )
/**/	{
/**/		throw new Error(
/**/			'undefined attribute view'
/**/		);
/**/	}
/**/
/**/	if( v_view === null )
/**/	{
/**/		throw new Error(
/**/			'view must not be null'
/**/		);
/**/	}
/**/	if( v_view.reflect !== 'View' )
/**/	{
/**/		throw new Error(
/**/			'type mismatch'
/**/		);
/**/	}
/**/}
	v_mark =
		Space.concernsMark(
			v_mark
		);


	if(
		inherit
		&&
		v_access === inherit.access
		&&
		v_hover.equals( inherit.hover )
		&&
		v_mark === inherit.mark
		&&
		v_path.equals( inherit.path )
		&&
		v_spaceTag === inherit.spaceTag
		&&
		v_spaceUser === inherit.spaceUser
		&&
		v_traitSet === null
		&&
		v_tree === inherit.tree
		&&
		v_view.equals( inherit.view )
	)
	{
		return inherit;
	}

	return (
		new Space(
			_tag,
			inherit,
			v_access,
			v_hover,
			v_mark,
			v_path,
			v_spaceTag,
			v_spaceUser,
			v_traitSet,
			v_tree,
			v_view
		)
	);
};


/*
| Reflection.
*/
Space.prototype.reflect =
	'Space';


/*
| Sets values by path.
*/
Space.prototype.setPath =
	JoobjProto.setPath;


/*
| Gets values by path.
*/
Space.prototype.getPath =
	JoobjProto.getPath;


/*
| Checks for equal objects.
*/
Space.prototype.equals =
	function(
		obj
	)
{
	if( this === obj )
	{
		return true;
	}

	if( !obj )
	{
		return false;
	}

	return (
		this.access === obj.access
		&&
		this.hover.equals( obj.hover )
		&&
		this.mark === obj.mark
		&&
		this.path.equals( obj.path )
		&&
		this.spaceTag === obj.spaceTag
		&&
		this.spaceUser === obj.spaceUser
		&&
		this.tree === obj.tree
		&&
		this.view.equals( obj.view )
	);
};


} )( );
