/*
| This is an autogenerated file.
|
| DO NOT EDIT!
*/

/*
| Export
*/

var
	HoverReply;

/*
| Imports
*/

var
	Jools;

/*
| Capsule
*/
(function( ) {
'use strict';

var
	_tag =
		'609783100';

/*
| Constructor.
*/
HoverReply =
	function(
		tag,
		cursor
		path
	)
{

/**/if( CHECK )
/**/{
/**/	if( tag !== _tag )
/**/	{
/**/		throw new Error(
/**/			'tag mismatch'
/**/		)
/**/	}

	this.cursor =
		cursor;

	this.path =
		path;

	Jools.immute( this );
};


/*
| Creates a new HoverReply object.
*/
HoverReply.create =
	function(
		// free strings
	)
{
	var

		cursor =
			null,

		inherit =
			null,

		path =
			null;

	for(
		var a = 0, aZ = arguments.length;
		a < aZ;
		a += 2
	)
	{
		switch( arguments[ a ] )
		{
			case 'cursor' :

				cursor =
					arguments[ a + 1 ];

				break;

			case 'inherit' :

				inherit =
					arguments[ a + 1 ];

				break;

			case 'path' :

				path =
					arguments[ a + 1 ];

				break;

			default :

/**/			if( CHECK )
/**/			{
/**/				throw new Error(
/**/					'invalid argument: ' + arguments[ a ]
/**/				);
/**/			}
		}
	}

	if( inherit )
	{
		if( cursor === null )
		{
			cursor =
				inherit.cursor;
		}

		if( path === null )
		{
			path =
				inherit.path;
		}
	}

};

} )( );
